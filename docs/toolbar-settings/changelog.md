# Changelog

Nothing implemented yet — the spec (`brief.md`, `context.md`, `plan.md`) is awaiting
review.

This file is the running log of what actually happens once work starts: what went wrong,
which assumptions turned out false, and what was left undone. It is written during the
work rather than after it, so it can record the wrong turns as well as the result.
